$(function() {
$(document).tooltip(); // tooltip  (on contact page's phone number element).
$( "#tabs" ).tabs(); //tabs (used on services page). 
$("#datepicker").datepicker(); //date picker (used on contact page). 
});
